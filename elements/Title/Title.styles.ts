import { StyleSheet } from 'react-native';
import { NAVY_BLUE } from '../../src/util/colors';

export default StyleSheet.create({
	text: {
		fontFamily: 'open-sans-bold',
		fontSize: 38,
		lineHeight: 50,
		color: NAVY_BLUE,
	},
});